{
    "builds": [{
        "src": "URLShortener/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "URLShortener/wsgi.py",
            "dest": "URLShortener/wsgi.py"
        }
    ]
}
